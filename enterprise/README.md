# Enterprise documentation

Placeholder for Enterprise-tier docs (SLAs, deployment guides, support
channels). Per the project scope, RBAC/SSO/audit-logging are explicitly
**not** implemented — Enterprise here currently means: higher seat counts
and priority support on top of the same Pro feature set (see
`LICENSE.enterprise`), not a separate feature tier.
